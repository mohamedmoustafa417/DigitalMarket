{
  "name": "digitalmarket-functions",
  "version": "1.0.0",
  "description": "Firebase Cloud Functions for DigitalMarket",
  "engines": { "node": "22" },
  "main": "index.js",
  "scripts": {
    "lint": "eslint --ext .js .",
    "serve": "firebase emulators:start --only functions",
    "deploy": "firebase deploy --only functions"
  },
  "dependencies": {
    "firebase-admin": "^13.0.0",
    "firebase-functions": "^6.0.0"
  },
  "devDependencies": {
    "eslint": "^8.15.0",
    "eslint-config-google": "^0.14.0"
  },
  "private": true
}
