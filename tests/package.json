{
  "name": "digitalmarket-e2e",
  "private": true,
  "version": "1.0.0",
  "description": "Smoke tests for digitalmarketstore.shop — guards every push against regressions.",
  "scripts": {
    "test": "playwright test",
    "test:headed": "playwright test --headed",
    "report": "playwright show-report"
  },
  "devDependencies": {
    "@playwright/test": "^1.48.0"
  }
}
